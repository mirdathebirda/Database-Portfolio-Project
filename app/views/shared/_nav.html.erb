    <div class="hidden-xs">
        <nav class='navbar-inner-header'>
            <div class='container'>
                <div class="collapse navbar-collapse navHeaderCollapse">
                    <ul class="nav navbar-nav navbar-left">
                        <li>
                            <a href="/" class='brand'>Not Blogger
                                <i class="fa fa-pencil fa-lg"></i>
                            </a>
                        </li>
                        <% if current_user %>
                        <li>
                            <%= link_to('Create New Blog', '/blogs/new', :class => 'nav nav-tabs')  %>
                        </li>
                        <% if !current_user.blogs.empty? %>
                        <li>
                            <%= link_to('Manage Blogs', "/users/#{current_user.id}/blogs", :class => 'nav nav-tabs')  %>
                        </li>
                        <% end %>
                        <% end %>
                        <li>
                            <%= link_to('Browse Blogs', '/blogs', :class => 'nav nav-tabs')  %>
                        </li>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        <% if current_user %>
                        <li >
                          <%= link_to(current_user.name, "/users/#{current_user.id}/blogs", :class => 'nav nav-tabs')  %>
                            <!-- <%= current_user.name %> -->
                        </li>
                        <li>
                            <%= link_to('Logout', '/logout', :class => 'nav nav-tabs')  %>
                        </li>
                    <% else %>
                        <li>
                            <%= link_to('Login', '/login', :class => 'nav nav-tabs')  %>
                        </li>
                        <li>
                            <%= link_to('Sign Up', '/signup', :class => 'nav nav-tabs')  %>
                        </li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </nav>
    </div>
    <div class="navbar navbar-default navbar-static-top visible-xs" style="margin-top:0">
        <div class="container">
            <div class="navbar-header">
                <a href="#" class="navbar-brand"> Not Blogger</a>
                <button class="navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
                  <!--  makes hamburger lmfao -->
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse navHeaderCollapse">
                <ul class="nav navbar-nav navbar-left">
                    <% if @current_user %>
                    <li>
                        <div class="nav nav-tabs">
                          <%= current_user.name %>
                        </div>
                    </li>
                    <li>
                        <%= link_to('Logout', '/logout', :class => 'nav nav-tabs')  %>
                    </li>
                <% else %>
                    <li>
                        <%= link_to('Login', '/login', :class => 'nav nav-tabs')  %>
                    </li>
                    <li>
                        <%= link_to('Sign Up', '/signup', :class => 'nav nav-tabs')  %>
                    </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
