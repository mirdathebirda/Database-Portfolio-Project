<div class="hidden-xs">
    <nav class='navbar-inner header'>
      <div class='container'>
        <div class="collapse navbar-collapse navHeaderCollapse">
          <ul class="nav navbar-nav navbar-left">
            <li>
              <a href="/" class='brand'>Not Blogger <i class="fa fa-pencil fa-lg"></i>
              </a>
            </li>
            <li>
              <%= link_to('Create new blog', '/blogs/new', :class => 'btn btn-default')  %>
            </li>
            <% if current_user && !current_user.blogs.empty? %>
              <%= link_to('Manage blogs', "/users/#{current_user.id}/blogs", :class => 'btn btn-default')  %>
            <% end %>
            <%= link_to('Browse blogs', '/blogs', :class => 'btn btn-default')  %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
              <li><div class="btn btn-default"><%= current_user.name %></div></li>
              <li>
                <%= link_to('Logout', '/logout', :class => 'btn btn-default')  %>
              </li>
            <% else %>
              <li>
                <%= link_to('Login', '/login', :class => 'btn btn-default')  %>
              </li>
              <li>
                <%= link_to('Sign up', '/signup', :class => 'btn btn-default')  %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
</div>
<div class="navbar navbar-default navbar-static-top visible-xs" style="margin-top:0">
    <div class="container">
        <div class="navbar-header">
            <a href="#" class="navbar-brand">Not Blogger</a>
            <button class="navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button></div>
            <div class="collapse navbar-collapse navHeaderCollapse">
              <ul class="nav navbar-nav navbar-left">
                <% if @current_user %>
                  <li><div class="btn btn-default"><%= current_user.name %></div></li>
                  <li>
                    <%= link_to('Logout', '/logout', :class => 'btn btn-default')  %>
                  </li>
                <% else %>
                  <li>
                    <%= link_to('Login', '/login', :class => 'btn btn-default')  %>
                  </li>
                  <li>
                    <%= link_to('Sign up', '/signup', :class => 'btn btn-default')  %>
                  </li>
                <% end %>
              </ul>
            </div>
        </div>
    </div>